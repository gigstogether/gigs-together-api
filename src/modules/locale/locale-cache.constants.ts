/** Fallback locale for Accept-Language resolution when no match or empty active set. */
export const LOCALE_DEFAULT_ISO = 'en';

/** Default active-locales cache TTL when LOCALE_ACTIVE_CACHE_TTL_MS is unset (1 hour). */
export const LOCALE_ACTIVE_CACHE_DEFAULT_TTL_MS = 3_600_000;
